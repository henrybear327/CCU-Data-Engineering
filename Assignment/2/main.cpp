#include <bits/stdc++.h>

using namespace std;

int main()
{
	freopen("gram.dms", "r", stdin);
	char buffer[40000];
	while(fgets(buffer, 10000, stdin) != NULL)
		printf("%s\n", buffer);

	return 0;
}
